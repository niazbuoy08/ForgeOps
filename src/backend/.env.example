# Local development only. Never commit a real .env file with real credentials.
ENVIRONMENT=dev
APP_VERSION=0.0.0-local
CORS_ALLOWED_ORIGINS=http://localhost:5173
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appdb
DB_USER=postgres
DB_PASSWORD=postgres
