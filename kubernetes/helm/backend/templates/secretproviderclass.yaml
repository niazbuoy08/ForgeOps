{{- if .Values.secretsStoreCsi.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "backend.fullname" . }}-secrets
  labels:
    {{- include "backend.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    userAssignedIdentityID: {{ .Values.secretsStoreCsi.userAssignedIdentityID | quote }}
    keyvaultName: {{ .Values.secretsStoreCsi.keyvaultName | quote }}
    tenantId: {{ .Values.secretsStoreCsi.tenantId | quote }}
    objects: |
      array:
        {{- range .Values.secretsStoreCsi.secretObjects }}
        - |
          objectName: {{ .objectName }}
          objectType: secret
        {{- end }}
  # Mirrors Key Vault secrets into a native Kubernetes Secret so they can be
  # consumed via envFrom, in addition to being available as mounted files.
  secretObjects:
    - secretName: {{ include "backend.fullname" . }}-db-credentials
      type: Opaque
      data:
        {{- range .Values.secretsStoreCsi.secretObjects }}
        - objectName: {{ .objectName }}
          key: {{ .key }}
        {{- end }}
{{- end }}
